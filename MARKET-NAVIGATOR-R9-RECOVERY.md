# Market Navigator Gate 4 R9 — Recovery Contract

Status: AUTHORITATIVE CORRECTION FOR R9
Date: 2026-09-02

This correction supersedes any conflicting V1/V2/V3/V4/V5 navigation language in earlier Gate 4 planning until it is folded back into the canonical Master Plan.

## Owner-confirmed analytical progression

- Breadcrumbs are at the top of the analytical surface.
- V1 and V2 are **not stacked**. V2 replaces V1 on the same NOW chart surface.
- V1 = Market overview showing Risk / Growth / Macro.
- Selecting Risk, Growth, or Macro replaces V1 with that exact V2 index/component view.
- Selecting a V2 component opens V3 as an **open modal analysis model** over the current context.
- There is **no V4 product state**.
- V5 is **strictly exploration launched from EXPLORE**. It is not part of the NOW V1→V2→V3 progression.
- No implementation may invent alternate cards, anchors, intermediate pages, or stacked V1/V2 layouts.

## Data integrity

- Owner-visible charts must use repository canonical evidence only.
- Synthetic/fake/random fallback chart data is prohibited.
- If canonical evidence is absent for a horizon, show an explicit empty/degraded state; never fabricate points to make the chart appear populated.
- Canonical browser reads for R9 use `market-data/manifest.json`, `market-data/indexes/*.json`, and `market-data/series/*.json` generated by the repository database build.

## R8 rejection / graveyard entry

`market-view-gate4-r8.html` is rejected in full and must not be used as an implementation ancestor.

Observed failures:
- stacked V1 over V2 instead of V2 replacing V1;
- incorrect navigation/progression despite repeated owner specification;
- V3 implemented as a page rather than the required open modal model;
- V5 lineage misrepresented;
- generated synthetic fallback evidence made populated charts appear real;
- MTD/horizon behavior therefore could not be trusted.

Root cause: R8 optimized for a superficially complete candidate instead of preserving the owner-defined interaction model and canonical evidence contract.

Recovery rule: R9 must implement the owner-confirmed progression above directly and must fail visibly on missing canonical data rather than inventing data.
