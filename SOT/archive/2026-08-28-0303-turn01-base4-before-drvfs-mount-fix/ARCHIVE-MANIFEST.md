# SOT patch archive

Archived before the Base-5 drvfs mount-verification correction on 2026-08-28 03:03 PT.

Affected canonical state immediately before this patch:
- `install-SOT-turn01-base.sh` blob `fc07e6e510048429ad22d35404753d7aea8b3909` (Base-4 installer)
- Base-4 backend is generated by the installer from the Base-3 integrator plus `patch-SOT-turn01-base-powershell.py`.
- Live runtime rolled back successfully to `2026.08.27.sot-turn01-base-2` after Base-4 failed its D: folder-browse gate.

Failure evidence:
- Windows drive discovery succeeded: `C,D,E,F,G,I,Q`.
- Base-4 incorrectly reported D:/F:/I: as mounted before a valid drvfs verification.
- D: constrained folder browse returned HTTP 404.

Correction contract:
- A Windows drive counts as mounted only when `/mnt/<letter>` is an actual mount target with filesystem type `drvfs` and source matching `<LETTER>:`.
- Placeholder directories under `/mnt` must never count as mounted drives.
- Clicking/browsing D:, F:, or Q: must lazy-mount the real Windows drive, return its real folders, and permit selecting or creating a folder.

Standing rule: archive affected current SOT state under `SOT/archive/` before every patch.
