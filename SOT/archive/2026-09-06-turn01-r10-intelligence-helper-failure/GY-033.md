# GY-033 — R10 intelligence used nonexistent backend helper

**Status:** REJECTED PRE-CUTOVER CANDIDATE
**Decision date:** 2026-09-06

## Evidence

Owner qualification run at 13:35 PDT using installer commit `9345cd3f01cf4f8ef908f991f55d7960532e99cb` stopped before cutover.

Passed: REQUIRE_TOOLS, DATABASE_INTEGRITY schema=5, LIVE_BACKEND, ARCHIVE_PRECHANGE, DEV_BACKEND parse, DEV_UI parse.

Behavioral intelligence fixture failed with:

`ReferenceError: one is not defined`

at `storageIntelligence()` in the generated temporary backend.

Archive path on host:
`/home/support/.openclaw/workspace/https/report/SOT/archive/20260906-133508-turn01-r10-operating-intelligence-release`

## Root cause

The R10 intelligence integrator called a nonexistent `one()` query helper. The governed backend exposes `rows(query)`. Syntax qualification could not detect this because the symbol is valid JavaScript but undefined at runtime.

## Required correction

Keep the failed generated candidate as evidence only. Correct the clean R10 integrator so the singleton summary query uses `rows(query)[0] || {}`. Retain the behavioral intelligence fixture as a mandatory Developer gate so the error is caught before cutover.

Do not patch the failed generated backend or UI forward.
