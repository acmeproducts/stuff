# GY-039 — R12 Manager qualifier asserted backend tag shape against generated HTML

Date: 2026-09-07

Corrected R12 installer commit `0f5510aa85ed677c927faba47131d0b3bdc2fd78` passed the Developer migration, backend behavior and UI parse gates, then failed before cutover in the Manager static contract check:

`AssertionError: direct:true`

The R12 backend does emit tag provenance objects with `direct:true` / `direct:false`, but the generated HTML does not contain the literal source text `direct:true`. The UI consumes the provenance through `let inherited=!t.direct`, renders inherited tags with the `inherited` chip class, labels their provenance as `Inherited from ...`, and routes removal to the originating folder assignment.

This was therefore a qualifier artifact-scope defect, not an implementation failure.

Host evidence:

`/home/support/.openclaw/workspace/https/report/SOT/archive/20260907-002513-turn01-r12-ssot-virtual-volume-release`

No cutover occurred. Live R11 backend, UI and schema-5 database remain unchanged.

Required correction:

- keep R12 schema/backend/UI source lineage unchanged;
- replace the invalid HTML assertion `direct:true` with UI-level provenance assertions that actually exist in the generated surface, including `let inherited=!t.direct` and `Inherited from`;
- retain the Developer behavioral inheritance fixture as the authoritative proof that folder tags flow to descendants;
- continue Developer → Manager → Red-team qualification before cutover;
- do not patch generated HTML or create a new implementation lineage.
