# GY-028 — R9 qualifier built an incomplete temporary runtime

Date: 2026-09-06
Status: QUALIFICATION FAILURE — PRE-CUTOVER

Owner ran canonical installer commit `0f377dcace2766ee31779499f307af3016349d1b`.

Passed before failure:
- REQUIRE_TOOLS
- DATABASE_INTEGRITY schema=5
- LIVE_BACKEND `2026.09.03.sot-turn01-coordination-2` schema=5
- ARCHIVE_PRECHANGE
- R9_BACKEND_PARSE
- R9_UI_CONTRACT

The temporary R9 backend was placed under a fresh temp directory whose `sot-db/migrations` directory was empty. `ensureSchema()` correctly rejected the temporary runtime because its migration set did not match the copied schema-5 database.

The installer also used a raw file copy for the live WAL-mode SQLite database instead of SQLite's consistent backup mechanism.

No cutover occurred. The live R8 runtime remained unchanged.

## Governing correction

Keep the R9 candidate unchanged. Correct only the mechanical qualifier:

1. Copy the live qualified migration files into the temporary runtime before requiring the backend.
2. Create the temporary database using SQLite `.backup`, not `cp`, so WAL state is included consistently.
3. Re-run R9 database and active-progress behavior gates before any cutover.
