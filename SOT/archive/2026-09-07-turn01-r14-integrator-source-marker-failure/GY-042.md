# GY-042 — R14 pre-cutover integrator source-marker failure

**Date:** 2026-09-07

## Status

Rejected pre-cutover candidate. Qualified R13 remains live. No cutover occurred.

## Failure

The R14 installer reached the governed R13 composition, then the R14 UI integrator exited with:

`unexpected integrator source marker`

Host evidence:

`/home/support/.openclaw/workspace/https/report/SOT/archive/20260907-021247-turn01-r14-mobile-first-tag-release`

## Cause

`integrate-SOT-turn01-r14-mobile-tag-editor-ui.py` incorrectly searched the generated R13 HTML for the Python-source-only marker:

`insert=src.index('function dashboard(){')`

That marker exists in the R13 integrator source, not in its generated HTML output. The R14 integrator therefore failed before producing a candidate.

## Correction contract

Keep the R14 product/UI contract unchanged. Correct only the R14 integrator so it inserts the runtime mobile edit-lock listeners directly before the generated `function dashboard(){` marker that is present in R13 HTML.

Do not patch the failed generated candidate. Recompose from the pinned qualified R9 → R10 → R11 → R12 → R13 source lineage, apply the corrected R14 integrator once, then run Developer → Manager → Red-team qualification before cutover.
