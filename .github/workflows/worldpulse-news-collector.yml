name: WorldPulse RSS cache collector

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"
  push:
    branches: [main]
    paths:
      - "worldpulse-rss-collector.py"
      - ".github/workflows/worldpulse-news-collector.yml"

permissions:
  contents: write

concurrency:
  group: worldpulse-rss-cache
  cancel-in-progress: true

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Refresh per-source and combined news caches
        run: python3 worldpulse-rss-collector.py

      - name: Commit refreshed RSS cache
        run: |
          if [ -z "$(git status --porcelain -- worldpulse-source-cache.json worldpulse-news-cache.json)" ]; then
            echo "No WorldPulse RSS cache changes to commit."
            exit 0
          fi
          git config user.name "worldpulse-bot"
          git config user.email "worldpulse-bot@users.noreply.github.com"
          git add worldpulse-source-cache.json worldpulse-news-cache.json
          git commit -m "data: refresh WorldPulse RSS cache"
          git pull --rebase origin main
          git push origin HEAD:main
