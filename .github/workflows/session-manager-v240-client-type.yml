name: Session Manager v2.4.0 client type

on:
  push:
    branches: [main]
    paths: [.github/workflows/session-manager-v240-client-type.yml]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify governed v2.3.5 input
        shell: bash
        run: |
          set -euo